/** Stable action/callback ids shared by the renderer and the handler registry. */

/** Button actionId for an approve click; the requestId rides in the button value. */
export const ACTION_APPROVE = "hitldev_approve";
/** Button actionId for a deny click; the requestId rides in the button value. */
export const ACTION_DENY = "hitldev_deny";
/** Button actionId for a batch submit; the batchId rides in the button value. */
export const ACTION_BATCH_SUBMIT = "hitldev_batch_submit";
/** Modal callbackId for the feedback form opened from an approve click. */
export const MODAL_CALLBACK = "hitldev_modal";

/** RadioSelect option values for a confirm field. */
export const CONFIRM_YES = "yes";
export const CONFIRM_NO = "no";
